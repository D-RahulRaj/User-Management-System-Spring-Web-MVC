# User Management System – Spring Web MVC

A complete User Management System built using Spring Web MVC, JSP, Servlets, and JDBC/Hibernate (as per your setup).  
The project follows the **Model–View–Controller (MVC)** architecture and provides end-to-end user CRUD operations.

## ⭐ Features
- Add, update, delete, and list users
- Spring Controllers to handle user requests
- JSP pages for user interface
- Form validation for preventing invalid inputs
- Clean MVC layered architecture
- Error handling and success message pages

## 🛠 Tech Stack
- Spring Web MVC  
- JSP, JSTL  
- Java  
- JDBC/Hibernate (based on your implementation)  
- Tomcat Server  

## 📁 Project Structure
- `Controller` – Handles incoming requests  
- `Service` – Business logic  
- `DAO` – Database operations  
- `JSP` – Views/UI  

## 🚀 How to Run
1. Import into **Eclipse**
2. Configure **Tomcat Server**
3. Run the project on server
4. Access on browser:  
   `http://localhost:8080/User_Management_System_Using_SpringWeb_MVC/`

